#include <stdio.h>
#include <limits.h>
int main()
{
    int n;
    scanf("%d",&n);
    int arr[n];
    printf("Enter array elements:\n");
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int arr2[n];
    for(int i=0;i<n;i++){
        arr2[i]=arr[i];
    }
    printf("Array elements are:\n");
    for(int i=0;i<n;i++){
        printf("%d",)
    }
}